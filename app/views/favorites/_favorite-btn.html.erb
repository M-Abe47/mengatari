<% if post.favorited_by?(current_user) %>
  <%= link_to post.favorites.count, post_favorites_path(post), method: :delete %>
<% else %>
  <%= link_to post.favorites.count, post_favorites_path(post), method: :post %>
<% end %>